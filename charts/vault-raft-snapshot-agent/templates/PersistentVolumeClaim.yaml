apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: vault-raft-snapshot-agent-storage
  namespace: vault
spec:
  accessModes:
    - ReadWriteMany
  resources:
    requests:
      storage: "4Gi"
