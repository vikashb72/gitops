apiVersion: v1
kind: Secret
metadata:
  name: vault-auth-secret
  namespace: vault
  annotations:
    kubernetes.io/service-account.name: vault-raft-snapshot-agent
type: kubernetes.io/service-account-token
