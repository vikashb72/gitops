apiVersion: networking.istio.io/v1alpha3
kind: VirtualService
metadata:
  name: argocd-virtualserivce
spec:
  hosts:
    - argocd.k8s.minikube.home.where-ever.za.net
  gateways:
    - istio-system/internal-ingress
  http:
    - route:
      - destination:
          host: argocd-server.argocd.svc.cluster.local
          port:
            number: 80
