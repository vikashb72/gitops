redis:
  global:
    redis:
  auth:
    enabled: false
    sentinel: true
  commonConfiguration: |
    maxmemory-policy allkeys-lru
    maxmemory 8mb
    appendonly yes
    save ""
  master:
    resourcesPreset: "nano" 			# nano, micro, small, medium
    kind: StatefulSet
    #persistence: false
  replica:
    resourcesPreset: "nano"
    kind: StatefulSet
    replicaCount: 2
    #persistence: false
  sentinel:
    resourcesPreset: "nano"
    kind: StatefulSet
    #persistence: false
