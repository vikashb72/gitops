redis:
  global:
    redis:
    password: ""
  auth:
    enabled: false
    sentinel: true
  master:
    resourcesPreset: "nano"
    kind: StatefulSet
  replica:
    resourcesPreset: "nano"
    kind: StatefulSet
    replicaCount: 2
  sentinel:
    resourcesPreset: "nano"
    kind: StatefulSet
  # nano, micro, small, medium
