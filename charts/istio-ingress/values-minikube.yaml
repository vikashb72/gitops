gateways:
  echoserver-gateway:
    namespace: echoserver
    gateway: istio
    listeners: 
      - name: http
        port: 80
        proto: HTTP
        allowedRoutes:
          namespaces:
            from: Same
    httproutes:
      - name: echoserver
        rules:
          - matches:
              - path: Exact
                value: /
            backendRefs:
              - name: echoserver
                port: 80
