gateways:
  httpbin-gateway:
    namespace: istio-ingress
    gateway: istio
    listeners:
      - name: httpbin-http
        hostname: "httpbin.k8s.minikube.home.where-ever.za.net"
        port: 80
        protocol: HTTP
        allowedRoutes:
          namespaces:
            from: All
    httproutes:
      - name: httpbin
        namespace: httpbin
        hostnames: '["httpbin.k8s.minikube.home.where-ever.za.net"]'
        rules:
        - matches:
          - path:
              type: PathPrefix
              value: /get
          backendRefs:
          - name: httpbin
            port: 8000
