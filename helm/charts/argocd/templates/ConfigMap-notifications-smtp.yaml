#apiVersion: v1
#kind: ConfigMap
#metadata:
#  name: argocd-notifications-cm
#  namespace: argocd
#data:
#  # Configure your specific SMTP server settings
#  service.email.smtp: |
#    host: smtp.gmail.com  # Or your company SMTP relay (e.g., ://office365.com)
#    port: 587
#    from: your-alert-sender@gmail.com
#    username: $email-username
#    password: $email-password
#
#  # Define the explicit email body format
#  template.app-sync-failed-email: |
#    email:
#      subject: "ALERT: Argo CD Application {{.app.metadata.name}} Sync Failed"
#      body: |
#        Application '{{.app.metadata.name}}' has failed to synchronize.
#        
#        Status Details:
#        - Namespace: {{.app.spec.destination.namespace}}
#        - Cluster: {{.app.spec.destination.server}}
#        
#        View details directly in the dashboard:
#        {{.context.argocdUrl}}/applications/{{.app.metadata.name}}
#
#  # Bind the trigger condition to your custom email template
#  trigger.on-sync-failed: |
#    - when: app.status.operationState.phase in ['Error', 'Failed']
#      send: [app-sync-failed-email]
