apiVersion: external-secrets.io/v1
kind: ExternalSecret
metadata:
  name: db-zitadel
  namespace: {{ $.Release.Namespace }}
spec:
  refreshInterval: 4h
  secretStoreRef:
    name: external-vault-backend
    kind: ClusterSecretStore
  target:
    name: db-zitadel
    creationPolicy: Owner
    deletionPolicy: "Retain"
  data:
    - secretKey: password
      remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        key: "kv/{{ $.Values.schema.env }}/zitadel/db"
        metadataPolicy: None
        property: password
    - secretKey: username
      remoteRef:
        conversionStrategy: Default
        decodingStrategy: None
        key: "kv/{{ $.Values.schema.env }}/zitadel/db"
        metadataPolicy: None
        property: username
