{{- range $.Values.schema.poc }}
apiVersion: v1
kind: Service
metadata:
  name: {{ . }}
  namespace: {{ $.Release.Namespace }}
spec:
  ports:
    - port: 8080
      targetPort: 8080
      name: http
  selector:
    app: {{ . }}
---
{{- end }}
