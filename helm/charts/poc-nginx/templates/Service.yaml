{{- range $.Values.schema.poc }}
apiVersion: v1
kind: Service
metadata:
  name: {{ . }}-service
  namespace: {{ $.Release.Namespace }}
spec:
  selector:
    app: {{ . }}
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 80
      targetPort: 80
{{- end }}
