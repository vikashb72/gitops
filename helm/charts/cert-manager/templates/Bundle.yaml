apiVersion: trust.cert-manager.io/v1alpha1
kind: Bundle
metadata:
  name: {{ .Values.schema.env }}-ca-bundle
spec:
  sources:
    - secret:
        name: "reference-tls"
        key: "ca.crt"
  target:
    configMap:
      key: "{{ .Values.schema.env }}-ca-bundle.pem"
