vpa:
  recommender:
    enabled: true
    resources:
    podMonitor:
      enabled: true
      labels:
        release: kube-prometheus-stack
  
  updater:
    enabled: false
    podMonitor:
      enabled: true
      labels:
        release: kube-prometheus-stack
  
  admissionController:
    enabled: false
  
  metrics-server:
    enabled: false
